[project]
name = "skaled-func-tests"
version = "0.1.0"
description = "Functional test framework for skaled"
requires-python = ">=3.11"
license = "AGPL-3.0"

dependencies = [
    "web3>=6.0,<8",
    "pytest>7",
    "sgx.py>=0.11",
    "eth-account>=0.10",
    "tomli>=2.0; python_version < '3.11'",
]

[project.optional-dependencies]
dev = [
    "ruff",
    "mypy",
]

[project.scripts]
func-test = "run:main"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
