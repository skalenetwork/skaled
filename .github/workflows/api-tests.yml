name: API tests
on:
  push:
    branches-ignore:
      - 'master'
      - 'beta'
      - 'stable'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

env:
  NO_ULIMIT_CHECK: 1
  NO_NTP_CHECK: 1
  TARGET: all
  CMAKE_BUILD_TYPE: Debug

jobs:
  api-tests:
    name: API tests
    runs-on: ubuntu-22.04

    steps:
      # Checkout the last pre-merge BITE tag so the BITE binary reflects the
      # original BITE codebase (before BITE and BITE2 were unified).
      - name: Checkout BITE tag 09ec35e for BITE build
        uses: actions/checkout@v6
        with:
          ref: '09ec35e5e'  # last commit on develop before BITE was merged with BITE2
          submodules: recursive

      - name: Install packages
        run: |
          sudo apt-get update
          sudo apt-get install -y libunwind-dev autoconf build-essential cmake libprocps-dev libtool texinfo wget yasm flex bison btrfs-progs python3 python3-pip gawk git vim doxygen
          sudo apt-get install -y make pkg-config libgnutls28-dev libssl-dev unzip zlib1g-dev libgcrypt20-dev gcc-11 g++-11 gperf clang-format-11 gnutls-dev
          sudo apt-get install -y nettle-dev libhiredis-dev redis-server google-perftools libgoogle-perftools-dev lcov libv8-dev
          sudo apt-get install -y gettext

          # Remove deps installed in the next steps
          sudo apt-get purge -y libbz2-dev liblz4-dev

      - name: Set gcc
        run: |
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-11 11
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-11 11
          sudo update-alternatives --install /usr/bin/gcov gcov /usr/bin/gcov-11 11
          sudo update-alternatives --install /usr/bin/gcov-dump gcov-dump /usr/bin/gcov-dump-11 11
          sudo update-alternatives --install /usr/bin/gcov-tool gcov-tool /usr/bin/gcov-tool-11 11
          gcc --version

      - name: Build dependencies
        run: |
          cd deps
          ./build.sh DEBUG=1 PARALLEL_COUNT=$(nproc)
          cd ..

      - name: Build BITE (tag 09ec35e, -DBITE=1)
        uses: ./.github/actions/cmake-build
        with:
          build_dir: build_bite
          cmake_build_type: ${{ env.CMAKE_BUILD_TYPE }}
          coverage: ${{ env.CODE_COVERAGE }}
          cmake_args: "-DBITE=1"
          make_target: skaled

      # Switch to the current branch. build_bite/ is untracked so it is
      # preserved by the checkout (clean: false). BITE2 is built with -DBITE=1
      # because BITE and BITE2 are unified in this branch.
      - name: Checkout current branch (preserves build_bite artifacts)
        uses: actions/checkout@v6
        with:
          ref: ${{ github.sha }}
          submodules: recursive
          clean: false

      - name: Build BITE2 (current branch, -DBITE=1)
        uses: ./.github/actions/cmake-build
        with:
          build_dir: build_bite2
          cmake_build_type: ${{ env.CMAKE_BUILD_TYPE }}
          coverage: ${{ env.CODE_COVERAGE }}
          cmake_args: "-DBITE=1"
          make_target: skaled

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install ts dependencies
        run: bun install --frozen-lockfile
        working-directory: test/api-tests/bite-compat/

      - name: API tests (bite-compat)
        uses: ./.github/actions/api-tests-run
        with:
          suite: bite-compat
        env:
          FUNC_TEST_PRIVATE_KEY: ${{ secrets.FUNC_TEST_PRIVATE_KEY }}
