name: Publish
on:
  pull_request:
    types: [closed]
    branches:
      - 'v*.*.*'
      - master
      - develop
      - beta
      - stable

  
jobs:
  configure:
    if: github.event.pull_request.merged == true
    name: Configure release
    runs-on: ubuntu-latest
    outputs:
      version_orig: ${{ steps.set-version.outputs.version_orig }}
      version_historic: ${{ steps.set-version.outputs.version_historic }}
      version_mirage: ${{ steps.set-version.outputs.version_mirage }}
      version_mirage_historic: ${{ steps.set-version.outputs.version_mirage_historic }}
      release_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Extract branch name
        id: extract_branch
        run: echo "BRANCH_NAME=${GITHUB_REF##*/}" >> $GITHUB_ENV

      - name: Read and calculate version
        id: set-version
        run: |
          VERSION=$(cat VERSION)
          VERSION=$(bash ./scripts/calculate_version.sh $BRANCH_NAME $VERSION)
          echo "Calculated version: $VERSION"

          ( test $BRANCH = "stable" && export PRERELEASE=false ) || export PRERELEASE=true
          echo "VERSION_ORIG=$VERSION" >> $GITHUB_ENV
          echo "VERSION_HISTORIC=$VERSION-historic" >> $GITHUB_ENV
          echo "VERSION_MIRAGE=$VERSION-mirage" >> $GITHUB_ENV
          echo "PRERELEASE=$PRERELEASE" >> $GITHUB_ENV

          echo "::set-output name=version_orig::$VERSION"
          echo "::set-output name=version_historic::$VERSION-historic"
          echo "::set-output name=version_mirage::$VERSION-mirage"
          echo "::set-output name=version_mirage_historic::$VERSION-mirage-historic"
          
      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@latest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.set-version.outputs.version_orig }}
          release_name: ${{ steps.set-version.outputs.version_orig }}
          draft: false
          prerelease: ${{ env.PRERELEASE }}

  build:
    needs: configure
    uses: ./.github/workflows/setup-build-publish.yml
    with:
      branch_name: ${{ github.ref_name }}
      image_version: ${{ needs.configure.outputs.version_orig }}
      build_type: "RelWithDebInfo"
      node_type: core
      release_url: ${{ needs.configure.outputs.release_url }}
      use_plain_version: true
    secrets: inherit

  build-historic:
    needs: configure
    uses: ./.github/workflows/setup-build-publish.yml
    with:
      branch_name: ${{ github.ref_name }}
      image_version: ${{ needs.configure.outputs.version_historic }}
      build_type: "RelWithDebInfo"
      node_type: historic
      release_url: ${{ needs.configure.outputs.release_url }}
      use_plain_version: true
    secrets: inherit
  
  build-mirage:
    needs: configure
    uses: ./.github/workflows/setup-build-publish.yml
    with:
      branch_name: ${{ github.ref_name }}
      image_version: ${{ needs.configure.outputs.version_mirage }}
      build_type: "RelWithDebInfo"
      node_type: mirage
      release_url: ${{ needs.configure.outputs.release_url }}
      use_plain_version: true
    secrets: inherit
  
  build-mirage-historic:
    needs: configure
    uses: ./.github/workflows/setup-build-publish.yml
    with:
      branch_name: ${{ github.ref_name }}
      image_version: ${{ needs.configure.outputs.version_mirage_historic }}
      build_type: "RelWithDebInfo"
      node_type: mirage-historic
      release_url: ${{ needs.configure.outputs.release_url }}
      use_plain_version: true
    secrets: inherit

  functional-tests:
    uses: ./.github/workflows/functional-tests.yml
    name: Functional testing for orig build
    needs: build
    with:
      version: ${{ needs.build.outputs.version }}
    secrets: inherit

  functional-tests-historic:
    uses: ./.github/workflows/functional-tests.yml
    name: Functional testing for historic build
    needs: build-historic
    with:
      version: ${{ needs.build-historic.outputs.version }}
    secrets: inherit
  
  functional-tests-mirage:
    uses: ./.github/workflows/functional-tests.yml
    name: Functional testing for mirage build
    needs: build-mirage
    with:
      version: ${{ needs.build-mirage.outputs.version }}
    secrets: inherit
  
  functional-tests-mirage-historic:
    uses: ./.github/workflows/functional-tests.yml
    name: Functional testing for mirage-historic build
    needs: build-mirage-historic
    with:
      version: ${{ needs.build-mirage-historic.outputs.version }}
    secrets: inherit
